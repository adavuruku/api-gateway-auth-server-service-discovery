spring:
  application:
    name: authentication-service
  cloud:
    config:
      import: optional:configserver:http://sherif:sherif!@localhost:8888
  profiles:
    active: production
server:
  port: 8555
management:
  endpoints:
    web:
      exposure:
        include: refresh